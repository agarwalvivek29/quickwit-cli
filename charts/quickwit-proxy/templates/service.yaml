apiVersion: v1
kind: Service
metadata:
  name: {{ include "quickwit-proxy.fullname" . }}
  labels:
    {{- include "quickwit-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quickwit-proxy.selectorLabels" . | nindent 4 }}
